snippet override
    override func ${1:name}(${2:#:arguments}) -> ${3:Int} {
        super.$1(${4})

        ${0}
    }

snippet overvoid
    override func ${1:name}(${2:#:arguments}) {
        super.$1(${3})

        ${0}
    }

snippet snpmake
    ${1}.snp_makeConstraints { make in
        ${0}
    }

snippet snpupdate
    ${1}.snp_updateConstraints { make in
        ${0}
    }

snippet snpremake
    ${1}.snp_remakeConstraints { make in
        ${0}
    }
