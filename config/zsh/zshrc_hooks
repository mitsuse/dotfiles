preexec() {
    # TODO: Send the name of command to be executed.
}

zshaddhistory() {
    local line=${1%%$'\n'}
    local cmd=${line%% *}
    [[\
        "${cmd}" != "openssl" &&
        "${cmd}" != "gm" &&
        "${cmd}" != "rm" &&
        "${cmd}" != "mv"
    ]]
}
